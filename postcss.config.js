/**
 * PostCSS configuration consumed by Vite during the build.
 *
 * `package.json` has `"type": "module"`, so this file is treated as an ES
 * module and uses `export default` rather than `module.exports`.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
